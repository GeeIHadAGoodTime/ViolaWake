# Live Smoke Results - 2026-05-06

## Commands

```bash
pytest tests/live/ --collect-only
VIOLAWAKE_LIVE=1 bash tests/live/run_smoke.sh
```

## Environment

- Site: `https://violawake.com`
- API: `https://api.violawake.com`
- API health precheck: `200`
- Site precheck: `200`
- Python: `3.11.9`
- pip: `25.2`
- Node: `v22.18.0`
- Playwright: `1.58.0`

## Collection

`pytest tests/live/ --collect-only` collected 47 tests.

## Final Smoke Summary

Final command exit code: `1`

Totals: 5 passed, 1 failed, 9 skipped, 32 deselected.

| Test | Result | Notes |
| --- | --- | --- |
| `test_health_endpoint` | PASS | `/api/health` returned 200. |
| `test_register_new_user_returns_token` | FAIL | Current live response is `429 Rate limit exceeded: 10 per 1 hour`. Earlier in the same smoke session, before the rate limit was exhausted, registration returned HTTP success but `token=""`, `user.id=0`, `email_verified=false`. |
| `test_register_duplicate_email_rejected` | SKIP | Blocked by live registration rate limit. |
| `test_login_bad_credentials_returns_401` | PASS | Bad login returned 401. |
| `test_me_with_token_returns_profile` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_billing_checkout_session_claim_endpoint` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_billing_checkout_actual_route_configuration` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_recordings_auth_required_and_fresh_user_list_empty` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_sql_injection_email_rejected_without_server_error` | PASS | Invalid SQL-like email rejected without a 5xx. |
| `test_path_traversal_file_route_not_served` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_jwt_tamper_rejected` | SKIP | Blocked by live registration rate limit/no usable token. |
| `test_resend_verification_delivery_or_auto_verify` | SKIP | Blocked by live registration rate limit/no usable token. Earlier pre-limit registration was not auto-verified. |
| `test_pip_install_core_smoke` | PASS | Clean venv installed `violawake`; version assertion `>=0.2.2` and import/VAD smoke passed. |
| `test_wasm_demo_route_reachable_or_local_dist_present` | SKIP | `/wasm-demo/` and `/demo` return 200 via SPA fallback, but not the WASM demo; local `wasm/dist` is absent. |
| `test_landing_renders_core_claims` | PASS | Landing page headline, comparison table, pricing preview, 102KB, and 10-sample claim rendered. |

## Critical Answers

1. Stripe configured live?
   - Not determined. The checkout probes could not reach billing because live registration did not produce a usable token before the registration rate limit was exhausted.
   - The suite includes both the claimed `/api/billing/checkout-session` probe and the source-backed `/api/billing/checkout` probe.

2. Resend configured live?
   - Not determined. Earlier pre-limit registration returned `email_verified=false`, so the backend did not auto-verify. No Mailosaur/webhook inbox was configured in this run, and subsequent attempts hit `429`.

3. `pip install violawake` working?
   - Yes for the smoke scope. A clean venv installed `violawake`, asserted version `>=0.2.2`, imported `WakeDetector`, and ran an RMS VAD silence check.
   - The heavier `violawake[all]` test is present in the full suite but was not part of smoke.

4. WASM browser SDK reachable on the live site?
   - No evidence that it is reachable. `/wasm-demo/` and `/demo` return `200`, but only through the Cloudflare Pages SPA fallback, not an actual WASM demo. Local `wasm/dist` was absent, so the fallback WASM test skipped.

## Main Findings

- Live registration is not usable as an auth bootstrap right now. Pre-rate-limit evidence showed HTTP success with an empty token and `user.id=0`; the final smoke then hit `429 Rate limit exceeded: 10 per 1 hour`.
- The deployed frontend and API are alive.
- Core PyPI install works in a clean venv for the smoke scope.
- The live WASM demo route is not actually served despite returning HTTP 200.
- Billing and email remain blocked behind auth/inbox prerequisites and need a rerun after the registration rate limit resets.
