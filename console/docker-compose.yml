services:
  backend:
    build:
      context: ..
      dockerfile: console/Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - backend-data:/app/data
      - ../src:/sdk/src:ro
    environment:
      - VIOLAWAKE_SECRET_KEY=dev-secret-change-in-production
      - PYTHONPATH=/sdk/src
      # - VIOLAWAKE_STRIPE_SECRET_KEY=sk_live_...
      # - VIOLAWAKE_STRIPE_WEBHOOK_SECRET=whsec_...
      # - VIOLAWAKE_STRIPE_PRICE_DEVELOPER=price_...
      # - VIOLAWAKE_STRIPE_PRICE_BUSINESS=price_...
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=/api
      - VITE_API_PROXY_TARGET=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy

volumes:
  backend-data:
